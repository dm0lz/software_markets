<%= form_with(model: [:admin, feature_extraction_query]) do |form| %>
  <% if feature_extraction_query.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(feature_extraction_query.errors.count, "error") %> prohibited this feature_extraction_query from being saved:</h2>

      <ul>
        <% feature_extraction_query.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.label :search_field, style: "display: block" %>
    <%= form.text_field :search_field %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
